// Seleciona todos os botões com a classe "vermais"
const buttons = document.querySelectorAll('.vermais');

// Adiciona um ouvinte de evento de clique a cada botão
buttons.forEach(button => {
    button.addEventListener('click', () => {
        alert('Em construção');
    });
});